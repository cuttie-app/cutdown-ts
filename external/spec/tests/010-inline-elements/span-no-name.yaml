id: span-no-name
section: "10.10"
description: ":: not followed by a valid [ID_LITERAL] name is emitted as literal Text(\"::\")"

input: |
  hello :: world

ast:
  - type: Paragraph
    children:
      - type: Text
        value: "hello :: world"
