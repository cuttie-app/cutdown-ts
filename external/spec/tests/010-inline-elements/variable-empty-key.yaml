id: variable-empty-key
section: "10.12"
description: "{{}} with empty key is invalid; emitted as literal text"

input: |
  {{}}

ast:
  - type: Paragraph
    children:
      - type: Text
        value: "{{}}"
