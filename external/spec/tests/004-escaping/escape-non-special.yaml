id: escape-non-special
section: "4"
description: Backslash before non-special character emits both characters literally

input: |
  \a

ast:
  - type: Paragraph
    children:
      - type: Text
        value: '\a'
